input: training_files.txt
output: output
graph: utils/pmt_graph.pt
stats: utils/norm_stats.npz

training:
  epochs: 100
  batch_size: 10
  lr: 0.001
  latent_dim: 64
  hidden_dim: 128
  patience: 10
  limit: 
  num_workers: 6
  loss: classification
  class_type: 5-label
  target: flavour